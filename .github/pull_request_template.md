## What this changes

<!-- One or two lines. -->

## Why

<!-- The problem it solves. Link an issue if there is one. -->

## Checklist

- [ ] `npm run typecheck`, `npm test` and `npm run format:check` pass
- [ ] `npm run build` succeeds and the extension still loads unpacked
- [ ] If this touches a rule: a test covers both a page that should fail it and
      a correct page that must not
- [ ] No new network requests, and no new permissions in `manifest.json`
